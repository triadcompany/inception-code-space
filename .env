# Frontend build-time config.
# Leave VITE_API_URL empty to use same-origin "/api" (nginx proxies it in prod,
# Vite dev server proxies it locally — see vite.config.ts / VITE_API_PROXY).
VITE_API_URL=
